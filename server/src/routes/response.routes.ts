import express from "express";
const router = express.Router();

// response routes
// Lire une response aléatoire par catégorie
// router.get("/", responseController.readResponses);
// router.get("/:id", responseController.readResponseById);

// // Créer une response
// router.post("/", responseController.createResponse);

// // Mettre à jour une response
// router.put("/:id", responseController.updateResponse);

// // Supprimer une response
// router.delete("/:id", responseController.deleteResponse);

// Exportez le routeur en utilisant la syntaxe ES6
export default router;
